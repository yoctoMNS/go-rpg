# 2026-08-26 — Day 01

| 項目 | 内容 |
| --- | --- |
| 計画 | `docs/plan/week-01.md` の Day 01 |
| 実作業時間 | 45分 |
| 状態 | 完了 |

## 1. やったこと

- `go mod init github.com/yoctoMNS/go-rpg` でモジュールを作成
- `go get github.com/hajimehoshi/ebiten/v2@latest` で Ebitengine v2.9.10 を導入
  - Ebitengine v2.9 は Go 1.25 以上が必要。`go.mod` の go ディレクティブは `1.25.0`
- `internal/config` に `Config` / `Default()` / `WindowSize()` を実装
- `internal/game` に `ebiten.Game` 実装（`Update` / `Draw` / `Layout`）と `Run()` を実装
- `cmd/rpg/main.go` は Composition Root（設定を作って `game.Run` を呼ぶだけ）
- `Makefile` に `run` / `build` / `test` / `check` / `log` を用意
- `scripts/new-log.sh` で作業ログを1コマンドで作れるようにした
- `.github/workflows/ci.yml` を追加（Linux では X11/OpenGL の開発ヘッダが必要）

## 2. 動作確認

`make run` で確認した内容:

- [x] 960x720 のウィンドウが開き、タイトルが `Go RPG`
- [x] 左上の `ticks` が増え続け、`TPS: 60.0` 前後が表示される
- [x] ウィンドウをリサイズしても、論理解像度 320x240 が保たれる

## 3. 詰まったこと / 学んだこと

- Linux で `go build` すると次のエラーが出る。Ebitengine が GLFW 経由で X11 を使うため。

  ```
  fatal error: X11/extensions/Xrandr.h: No such file or directory
  ```

  解決:

  ```sh
  sudo apt-get install -y libx11-dev libxrandr-dev libxcursor-dev \
      libxinerama-dev libxi-dev libxxf86vm-dev libgl1-mesa-dev
  ```

  macOS / Windows では不要。CI（ubuntu-latest）にも同じ手順を入れた。

- 論理解像度を返す `Layout` の意味を理解した。`Layout` が固定値を返す限り、ゲーム側のコードは
  常に 320x240 の座標系だけを考えればよく、拡大縮小は Ebitengine が担当する。

## 4. 次回やること

- Day 02: `internal/config/config_test.go` と `internal/game/game_test.go` を書く
  - `Layout` のテーブル駆動テスト（等倍 / 3倍 / アスペクト比違い）
  - 外部テストパッケージ（`package config_test`）で書く
- `make check` が通ることを確認する

## 5. 設計メモ

- `Draw` の中で `ticks` を増やしたくなったが、我慢して `Update` に置いた。
  Ebitengine は処理落ち時に `Draw` をスキップするため、`Draw` で状態を進めると
  重い環境でだけゲームが遅くなるバグになる。
- `main.go` にロジックを書かないと決めた。`main` は「組み立てて実行する」だけにしておくと、
  後でテスト用の別エントリポイント（例: ヘッドレスでバランス調整を回す）を足しやすい。
